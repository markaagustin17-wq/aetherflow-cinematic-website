const icons = require('lucide-react');
console.log(Object.keys(icons).filter(k => k.toLowerCase().includes('twitter') || k.toLowerCase().includes('linkedin') || k.toLowerCase().includes('brand') || k.toLowerCase().includes('x')));
